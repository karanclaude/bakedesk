# Required — Claude API key for the AI consultant
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Optional — Supabase for user auth and cloud sync
# App works fully without these (localStorage fallback)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
